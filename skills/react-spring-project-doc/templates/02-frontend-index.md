# Phase 2 — Frontend Index 模板

> 落盘到 `docs/.analysis/02-frontend-index.md`。
> 输入：`01-discovery-report.md` 定位出的前端目录。只看前端源码，**不要碰后端**。
> 每个会进入最终文档的结论，按 `schemas/evidence-record.md` 追加一条到 `docs/.analysis/evidence-ledger.md`。
> 路径写真实相对路径；推断的命名/职责标「推测」；不确定的进「待确认项」。
>
> **先跑种子（推荐）**：`node skills/react-spring-project-doc/scripts/extract-endpoints.js --project <项目根> --out docs/.analysis/endpoints-seed.json`，把其中 `frontend[]` 作为第 5 节 API 方法列表的**基线清单**。**种子是基线不是事实**：逐条 Grep 核对调用确实存在、补全被哪些页面调用与置信度；`confidence: needs-review` 的项（模板字符串/动态 URL）必须人工确认，不要直接当事实登记。

## 1. 前端入口

- 入口文件：`<path>`
- 根组件挂载：`<App 渲染位置 / Provider 包裹链>`
- 全局 Provider（状态/主题/i18n/鉴权）：`<逐个列出 path>`

## 2. 路由表

> 从路由定义文件提取。动态路由、懒加载、嵌套路由都要体现。

| 路由路径    | 页面组件          | 组件文件 | 是否懒加载 | 是否需鉴权     | 备注 |
| ----------- | ----------------- | -------- | ---------- | -------------- | ---- |
| `</orders>` | `<OrderListPage>` | `<path>` | <是/否>    | <是/否/待确认> | <…>  |

## 3. 页面模块列表

> 按业务把页面聚成模块（如「订单」「用户」），供 P5 选链路用。

| 业务模块 | 包含页面                  | 页面文件目录 | 一句话职责 | 置信度     |
| -------- | ------------------------- | ------------ | ---------- | ---------- |
| <订单>   | <OrderList / OrderDetail> | `<path>`     | <…>        | <高/中/低> |

## 4. 组件目录

- 通用/原子组件目录：`<path>`
- 业务组件目录：`<path>`
- 复用度高的关键组件：<列出 2~5 个 + path，仅复用信号明显的>

## 5. API 方法列表

> 这是 P4 映射的左半边。逐个列出前端发起请求的方法/函数。
> baseURL、统一前缀、proxy 改写一定要记清楚——P4 拼 URL 靠它。

- 请求实例/baseURL 定义：`<path>`，baseURL = `<值或来源>`
- 统一前缀/代理：`<vite proxy / 网关前缀 / 「无」>`

| 前端 API 方法   | 文件     | HTTP 方法 | 路径（拼接前原文） | 被哪些页面调用  | 置信度     |
| --------------- | -------- | --------- | ------------------ | --------------- | ---------- |
| `<getOrders()>` | `<path>` | GET       | `<'/api/orders'>`  | <OrderListPage> | <高/中/低> |

## 6. 状态管理

- 方案：<Redux/Zustand/Context/React Query/无>
- store/slice 位置：`<path 列表>`
- 服务端缓存（React Query/SWR）键策略：<如有，简述；无则「无」>

## 7. 权限与拦截器

- 权限处理位置：`<路由守卫 / 高阶组件 / hook，path>`
- 请求拦截器：`<path——做了什么：加 token / 加前缀 / 改 header>`
- 响应拦截器：`<path——做了什么：统一错误处理 / 解包 data / 401 跳登录>`

## 8. 环境变量与构建配置

| 变量/配置         | 含义       | 来源文件 | 备注 |
| ----------------- | ---------- | -------- | ---- |
| `<VITE_API_BASE>` | <后端地址> | `<.env>` | <…>  |

## 9. 初步业务模块判断

> 在第 3 节基础上收敛，标注置信度。这是 P5 选核心链路的候选池。

- <模块>：<职责一句话>（置信度 <高/中/低>，证据：<path>）

## 10. 待确认项

1. <如：某页面路由存在但未找到对应组件文件>
2. <如：baseURL 在运行时注入，静态无法确认>
